import React from "react";

const Detail = (props) => {

    
return <h3>평점 남기기</h3>;
};

{/* <Input>
<input type="text" ref={text} />
<button onClick={addMain}>추가하기</button>
</Input> */}

export default Detail;